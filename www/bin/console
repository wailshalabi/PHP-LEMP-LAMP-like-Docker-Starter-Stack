#!/usr/bin/env php
<?php

require __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use App\Command\StatusCommand;

$application = new Application('Docker PHP CLI', '1.0.0');

$application->addCommand(new StatusCommand());

$application->run();
